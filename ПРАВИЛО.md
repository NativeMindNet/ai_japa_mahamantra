# ПРАВИЛО - Руководство по имплементации AI в Mahamantra

## Интеграция с Мозгач108 и Flutter LLaMA

### Правило № 1: Последовательная обработка махамантры через квантовые модели

**Задача:**
Имплементировать запросы "Харе Кришна Харе Кришна Кришна Кришна Харе Харей Харе Рама Харе Рама Рама Рама Харей Харе" через flutter_llama и мозгач108, к каждой из 108 пост квантовых моделей последовательно.

**Требования:**

1. **Инфраструктура:**
   - Использовать `flutter_llama` как интерфейс для работы с локальными LLM моделями
   - Интегрировать с мозгач108 для доступа к 108 квантовым моделям
   - Обеспечить последовательную обработку через все 108 моделей

2. **Процесс обработки:**
   - На каждой из 108 четок (бусин) выполнять запрос к соответствующей квантовой модели
   - Передавать текст махамантры: "Харе Кришна Харе Кришна Кришна Кришна Харе Харей Харе Рама Харе Рама Рама Рама Харей Харе"
   - Собирать ответы от каждой модели

3. **Логирование:**
   - Результаты обработки записывать в специальный лог
   - Лог должен быть скрыт и доступен ТОЛЬКО через Режим разработчика
   - Лог должен содержать:
     - Номер модели (1-108)
     - Название модели
     - Timestamp запроса
     - Ответ модели
     - Время обработки

4. **Режим разработчика для доступа к логу:**
   - Активация через множественное нажатие на версию приложения (как в Android)
   - Как активировать:
     1. Откройте **Настройки** → **О приложении**
     2. Нажмите на **"Версия"** 7 раз подряд (в течение 3 секунд)
     3. Появится уведомление "🎉 Режим разработчика активирован!"
     4. Появится кнопка **"🔓 Просмотр логов"** с доступом к зашифрованным логам
   - Лог открывается в отдельном защищенном экране с AES-256 шифрованием

5. **Архитектура:**
   ```
   User Interaction → Session Manager
                    ↓
   Mahamantra Text (108 repetitions)
                    ↓
   Flutter LLaMA Service
                    ↓
   Мозгач108 Models (1-108) [Sequential Processing]
                   ↓
   Hidden Log Writer → Encrypted Storage
                   ↓
   Developer Mode (7 taps on version) → Log Viewer
   ```

6. **Технические детали:**
   - Модели должны загружаться по требованию (lazy loading)
   - Использовать кэширование для оптимизации
   - Обработка должна быть асинхронной, но последовательной
   - Предусмотреть обработку ошибок и пропуск недоступных моделей
   - Уведомлять пользователя о прогрессе (опционально, через вибрацию)

7. **Безопасность:**
   - Лог должен быть зашифрован
   - Доступ только через Режим разработчика (7 нажатий на версию)
   - Возможность очистки лога
   - Лог не должен передаваться никуда за пределы устройства

## Статус имплементации Правила № 1

- ✅ Интеграция flutter_llama
- ✅ Подключение к braindler моделям (7 моделей от Ollama с вариациями = 108 циклов)
- ✅ Система логирования (AES-256 шифрование)
- ✅ Режим разработчика (7 нажатий на версию в настройках)
- ✅ Шифрование лога (EncryptedLogService)
- ✅ Интеграция с Ollama API (https://ollama.com/nativemind/braindler)
- ✅ Оптимизация производительности

**Статус:** ✅ **РЕАЛИЗОВАНО**

**Файлы:**
- `lib/services/braindler108_service.dart` - Сервис для работы с 108 циклами через braindler
- `lib/services/mozgach108_service.dart` - Устаревший сервис (сохранен для совместимости)
- `lib/services/encrypted_log_service.dart` - Шифрование логов (AES-256)
- `lib/screens/easter_egg_logs_screen.dart` - Экран просмотра логов (режим разработчика)
- `lib/screens/settings_screen.dart` - Режим разработчика через нажатие на версию

---

## Правило № 2: Дефолтное расписание напоминаний

**Задача:**
Реализовать автоматические напоминания о джапе с дефолтным расписанием, которое включено по умолчанию.

**Расписание по умолчанию:**

**Будние дни (понедельник - пятница):**
- 🌅 **08:01** - Утреннее напоминание
- 🌙 **21:08** - Вечернее напоминание

**Выходные дни (суббота - воскресенье):**
- 🌅 **09:00** - Утреннее напоминание
- 🌙 **21:00** - Вечернее напоминание

**Требования:**

1. **Автоматическая активация:**
   - Расписание должно быть **включено по умолчанию**
   - Регистрация при первом запуске приложения через `BackgroundService.registerDefaultAutoSchedule()`
   - Фоновая проверка каждые 15 минут

2. **Защита от дублирования:**
   - Одно уведомление не может быть отправлено дважды в один день
   - Использование SharedPreferences для отслеживания последних уведомлений

3. **Управление:**
   - Возможность включить/выключить: `setAutoScheduleEnabled(bool)`
   - Проверка статуса: `isAutoScheduleEnabled()`

4. **Техническая реализация:**
   - Использование `workmanager` для фоновых задач
   - Задача `japa_schedule_check` выполняется каждые 15 минут
   - Проверка дня недели для выбора правильного расписания
   - Отправка уведомлений через `NotificationService`

**Статус:** ✅ **РЕАЛИЗОВАНО**

**Файлы:**
- `lib/services/background_service.dart` - Фоновые задачи и автоматическое расписание

---

## Правило № 3: Энергоэффективный режим при отсутствии AI ускорителя

**Задача:**
Если не доступен ИИ ускоритель, то включается энергоэффективный экспресс режим (по дефолту - включен именно High Power). Low Power - В цикле добавляет к строковой переменной предыдущей через стандартную конкатенацию одну мантру. Когда цикл 108 закончен - переменная отображается в Log через Режим разработчика

**Режимы работы:**

1. **High Power Mode (по умолчанию):**
   - Активируется при доступном AI ускорителе
   - Обработка через 108 моделей мозgач108
   - Максимальное качество ответов AI
   - Использует GPU/Neural Engine

2. **Low Power Mode (автоматически):**
   - Активируется при недоступности ускорителя
   - Простая конкатенация строк: `accumulator = accumulator + mantra + '\n'`
   - Цикл из 108 мантр
   - После завершения цикла → сохранение в зашифрованный лог
   - Минимальное энергопотребление
   - Просмотр через Режим разработчика

**Статус:** ✅ **РЕАЛИЗОВАНО**

**Файлы:**
- `lib/services/ai_power_mode_service.dart` - Управление режимами AI
- Автоматическое определение доступности ускорителя
- Логирование циклов в Low Power режиме

---

## Правило № 4: Воспевание при спящем режиме и на зарядке

**Задача:**
Реализовать режим постоянного воспевания для правильной утилизации свободных ресурсов устройства при подключении к зарядке и в спящем режиме.

**Концепция:**
Аналогично режиму разработчика "Keep screen on when charging" (экран не гаснет при подключенном питании), но для духовной практики - устройство будет непрерывно воспевать махамантру, пока оно на зарядке.

**Режимы работы:**

1. **Воспевание на зарядке (по умолчанию включено):**
   - Автоматический старт при подключении зарядки
   - WakeLock для предотвращения засыпания процессов
   - Непрерывное воспевание с настраиваемым интервалом (по умолчанию 30 секунд)
   - Уведомления о начале и завершении зарядки с статистикой
   - Мониторинг уровня батареи

2. **Воспевание в спящем режиме (опционально):**
   - Работа в фоновом режиме при выключенном экране
   - Минимальное энергопотребление
   - Использование Low Power режима для экономии батареи
   - Логирование в зашифрованный лог

**Технические особенности:**

1. **Мониторинг батареи:**
   - Использование `battery_plus` для отслеживания состояния зарядки
   - Автоматическое определение начала/окончания зарядки
   - Отслеживание уровня батареи

2. **WakeLock (аналог Developer "Stay awake"):**
   - Использование `wakelock_plus` для предотвращения засыпания
   - Включается только при активном воспевании
   - Автоматически отключается при отключении зарядки

3. **Два режима обработки:**
   - **High Power Mode**: Обработка через 108 моделей Мозgач108 (если доступен AI ускоритель)
   - **Low Power Mode**: Простая конкатенация строк для минимального энергопотребления

4. **Логирование:**
   - Все воспевания записываются в зашифрованный лог (AES-256)
   - Доступ через Режим разработчика (7 нажатий на версию)
   - Статистика:
     - Timestamp воспевания
     - Режим (на зарядке/спящий)
     - Уровень батареи
     - Время обработки
     - Результат AI или Low Power
     - Общее количество воспеваний

5. **Настройки:**
   ```dart
   // Включить/выключить воспевание на зарядке
   await ChargingChantingService.instance.setChargingChantingEnabled(true);
   
   // Включить/выключить воспевание в спящем режиме
   await ChargingChantingService.instance.setSleepChantingEnabled(true);
   
   // Установить использование AI моделей
   await ChargingChantingService.instance.setUseAIModels(true);
   
   // Установить интервал (секунды)
   await ChargingChantingService.instance.setChantingInterval(30);
   ```

**Архитектура:**
```
Зарядка подключена → Battery State Changed
                   ↓
         WakeLock активирован
                   ↓
    Timer (каждые N секунд)
                   ↓
         Воспевание мантры
        /                  \
High Power Mode        Low Power Mode
(Мозgач108)           (Конкатенация)
        \                  /
                   ↓
      Encrypted Log Storage
                   ↓
         Developer Mode Access
```

**Энергоэффективность:**

1. **На зарядке:**
   - Полноценное использование AI моделей (если доступны)
   - WakeLock для непрерывной работы
   - Отсутствие ограничений по ресурсам

2. **В спящем режиме:**
   - Приоритет Low Power режиму
   - Минимальная частота воспеваний
   - Автоматическая адаптация под уровень батареи

**Уведомления:**

1. **При подключении зарядки:**
   ```
   🔋 Зарядка началась - воспевание активировано
   Устройство на зарядке. Махамантра будет воспеваться 
   автоматически. Харе Кришна!
   ```

2. **При отключении зарядки:**
   ```
   🔋 Зарядка завершена
   Время на зарядке: 120 мин
   Воспевано мантр: 240
   Харе Кришна!
   ```

**Статистика:**
```dart
final stats = ChargingChantingService.instance.getStatistics();
print('Статус: ${stats['status']}');
print('На зарядке: ${stats['is_charging']}');
print('Батарея: ${stats['battery_level']}%');
print('Всего воспеваний: ${stats['total_chants']}');
print('Воспеваний на зарядке: ${stats['chants_during_charging']}');
print('Последнее: ${stats['last_chant_time']}');
```

**Статус:** ✅ **РЕАЛИЗОВАНО**

**Файлы:**
- `lib/services/charging_chanting_service.dart` - Основной сервис воспевания
- `lib/services/background_service.dart` - Интеграция фоновых задач
- `lib/main.dart` - Инициализация при запуске

---

## 🎯 Итоговый статус всех правил

| Правило | Статус | Файлы | Описание |
|---------|--------|-------|----------|
| **№ 1** | ✅ **100%** | `braindler108_service.dart`<br>`encrypted_log_service.dart`<br>`easter_egg_logs_screen.dart`<br>`settings_screen.dart` | Обработка через 108 циклов braindler с логированием |
| **№ 2** | ✅ **100%** | `background_service.dart` | Автоматическое расписание напоминаний (будни 08:01/21:08, выходные 09:00/21:00) |
| **№ 3** | ✅ **100%** | `ai_power_mode_service.dart` | Auto-switch между High Power (AI) и Low Power (конкатенация) |
| **№ 4** | ✅ **100%** | `charging_chanting_service.dart`<br>`background_service.dart` | Воспевание на зарядке и в спящем режиме с WakeLock |

## 📚 Документация

- **ПРАВИЛА_ИСПОЛЬЗОВАНИЕ.md** - Подробное руководство по использованию всех правил
- **Примеры кода** - Интеграция в существующий проект
- **Troubleshooting** - Решение типичных проблем

## 🚀 Быстрый старт

```dart
// main.dart
import 'package:workmanager/workmanager.dart';
import 'services/background_service.dart';
import 'services/ai_power_mode_service.dart';
import 'services/mozgach108_service.dart';
import 'services/encrypted_log_service.dart';
import 'services/charging_chanting_service.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  
  // Инициализация Workmanager
  await Workmanager().initialize(callbackDispatcher);
  
  // Правило № 2: Автоматическое расписание
  await BackgroundService.registerDefaultAutoSchedule();
  
  // Правило № 3: AI Power Mode
  await AIPowerModeService.instance.initialize();
  
  // Правило № 1: Мозgач108
  await Mozgach108Service.instance.initialize();
  
  // Шифрованное логирование
  await EncryptedLogService.instance.initialize();
  
  // Правило № 4: Воспевание на зарядке
  await ChargingChantingService.instance.initialize();
  await BackgroundService.registerChargingChanting();
  
  runApp(MyApp());
}
```

## 🔐 Безопасность и доступ к логам

- **Шифрование**: AES-256 с уникальным ключом для каждого устройства
- **Изоляция**: Логи хранятся только локально
- **Доступ**: Через режим разработчика (7 нажатий на версию в настройках)
- **Активация режима разработчика**:
  1. Откройте **Настройки** → **О приложении**
  2. Нажмите на **"Версия"** 7 раз подряд (в течение 3 секунд)
  3. Появится уведомление: "🎉 Режим разработчика активирован!"
  4. В секции "О приложении" появится кнопка **"🔓 Просмотр логов"**
- **Индикация**: Иконка версии меняется на 🔓 с зеленой галочкой
- **Очистка**: Возможность полного удаления логов из экрана просмотра

## 📊 Мониторинг

```dart
// Статистика AI Power Mode
final aiStats = AIPowerModeService.instance.getStatistics();
print('Режим: ${aiStats['currentMode']}');
print('Ускоритель: ${aiStats['isAcceleratorAvailable']}');

// Статистика Мозgач108
final mozgachStats = await Mozgach108Service.instance.getStatistics();
print('Обработано моделей: ${mozgachStats['total_models_processed']}');

// Статистика логов
final logStats = await EncryptedLogService.instance.getLogsStatistics();
print('Всего записей: ${logStats['total_count']}');

// Статистика воспевания на зарядке (Правило № 4)
final chantingStats = ChargingChantingService.instance.getStatistics();
print('Воспевание активно: ${chantingStats['is_chanting']}');
print('На зарядке: ${chantingStats['is_charging']}');
print('Батарея: ${chantingStats['battery_level']}%');
print('Всего воспеваний: ${chantingStats['total_chants']}');
print('Воспеваний на зарядке: ${chantingStats['chants_during_charging']}');
```

---

## 🔋 Зависимости для Правила № 4

Добавьте в `pubspec.yaml`:

```yaml
dependencies:
  # Battery monitoring для воспевания на зарядке
  battery_plus: ^6.0.3
  
  # Wake lock для постоянного воспевания на зарядке
  wakelock_plus: ^1.2.8
```

---

*Дата создания: 19.10.2025*  
*Последнее обновление: 20.10.2025*  
*Версия: 1.3*  
*Статус: ✅ **ВСЕ ЧЕТЫРЕ ПРАВИЛА РЕАЛИЗОВАНЫ***

**Харе Кришна! 🕉️**

## 🙏 Духовное значение Правила № 4

Правило № 4 воплощает принцип **"непрерывного воспевания"** (ахайтуки, апратихата) - когда практика не прерывается внешними обстоятельствами. 

Даже когда устройство находится на зарядке или в спящем режиме, оно продолжает воспевать святые имена, тем самым:
- Освящая окружающее пространство
- Очищая эфир звуковыми вибрациями
- Использует технические ресурсы для высшей цели
- Демонстрирует, что духовная практика может быть постоянной

Это подобно тому, как преданные держат махамантру в сердце даже во сне - устройство "помнит" и продолжает воспевать независимо от своего состояния.

**Харе Кришна Харе Кришна Кришна Кришна Харе Харей**  
**Харе Рама Харе Рама Рама Рама Харей Харе** 🕉️

---

## 📦 Мультимодальный датасет Kene Multimodal Gift

**Датасет:** [nativemind/kene_multimodal_gift](https://huggingface.co/datasets/nativemind/kene_multimodal_gift)

### Структура датасета по категориям:

📁 **ИКАРОС МАЭСТРО ОРЛАНДО ШИПИБО - музыка**
   - Духовная музыка и песни ИКАРОС
   - Традиции шипибо
   - Целительские мелодии Амазонии

📁 **Джив Джаго** - духовная музыка (MP3, WAV)
   - Sadhu Sanga
   - Gurudev
   - Haribol
   - Chatur Sloka
   - Hanuman (master)

📁 **Детские игрушки** - фотографии игрушек для обучения AI

📁 **Детские рисунки** - детские рисунки для визуального обучения

📁 **Таиланд** - фотографии природы и путешествий
   - Природа
   
📁 **Тайные знания (не для всех)** - эзотерические материалы
   - Chamber Of Secrets (Гарри Поттер)

📁 **Фильмы (Трейлеры)** - трейлеры и саундтреки

📁 **Рилсы** - короткие видео

📁 **Дом** - домашние фото

📁 **Разное** - различные медиа файлы

### Назначение датасета

Мультимодальный датасет для тренировки и тестирования:
- Визуальных моделей (изображения)
- Аудио моделей (музыка, песни)
- Видео моделей (трейлеры, рилсы)
- Кросс-модальных трансформеров

**Формат:** Mixed (Images: JPG/JPEG, Audio: MP3/M4A/WAV, Video: MP4)  
**Размер:** ~1 GB  
**Файлов:** 50+
